
const validateForm  = express('../__test__/validation.test')//

describe('form validation test', () => {
    beforeEach(() => {
        document.body.innerHTML = 
    })

    test('should return an error for an empty name', () => {
        // Add your assertions here to verify the error//+
    })
})
